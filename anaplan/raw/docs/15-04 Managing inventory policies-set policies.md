# Managing inventory policies-set policies

Next we'll look at how we can use that to define the policies in place.

So we'll do that within the application.

And we'll move over to page 206.

Set inventory policies by segment.

Okay.

So in principle we can start to see the idea here, which is based upon that classification matrix where you're going to define the policies that are in operation.

So are Xs, we want to be reviewing every four weeks and setting a target of a period of supply.

So thinking about the magnitude and the volatility of the product as a way of defining the target that we want in place.

Just in, case or in this instance, the way my application is pre-configured.

I need to revert that to use, the new product location specific values that we have just determined.

So having done that, we can now see, an inventory projection being calculated based upon those, segments, both in terms of volumes and value, based upon the DCs.

If I move over to my main inventory planning page 240.

That is the basis for what's coming through here.

So that ABC XYZ segmentation, at the product location level, is what determines the default policy.

And then obviously we can still override that should we wish, as I have done here.

