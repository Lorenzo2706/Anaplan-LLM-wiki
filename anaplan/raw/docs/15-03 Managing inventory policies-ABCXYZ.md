# Managing inventory policies-ABCXYZ

The next thing we want to explore is, how we then use ABC XYZ as the way for, defining what policies apply.

So where I’ll go next actually is to this ABC XYZ section, and I'm going to look at this “Edit and Commit” page.

Essentially what we are doing here is, a process which is just the same as within demand analysis.

So in this case it's an ABC segmentation.

So we're ranking our products based upon a particular input into that rank that we rank in based upon history.

So know, what are our biggest products.

But in this instance we are specifically doing that as it relates to the particular DC.

So when we ranking this as a one, that's because it's the largest volume product for the selected DC.

And that may differ as we go through different DCS.

So this recognizes that ABC XYZ segmentation really needs to be evaluated at the DC level.

Just because something is a rank overall in total terms of total volumes, doesn't necessarily mean that it's a materially important product for a particular DC.

So having just done that, I’ll need to, commit that.

So again, it's just like we had on demand analysis where we see what's being calculated.

I can override that if I wanted to.

So for instance, here's that temporary product I created as part of the new product introduction process.

So maybe that needs to be flagged as an “A” because it's a strategic product that just has no volumes.

And then the effective settings which are in place, need to be saved via a process.

So this committing, this saving logic, really follows the rationale that, again, because of the relationship that these ABC categorizations have on inventory levels, they are something which are more likely going to be periodically reviewed by the business rather than just dynamically changing based upon the data that gets loaded in.

So that's completed things.

So we now have those committed ABC's where I will now go is to move over to the 158 page and do exactly the same process for our XYZ.

So again here, same idea.

Let's analyze the variability of demand for each product, but specifically as it relates to this selected DC.

With that analysis being dynamically calculated, we can then override should we wish.

So we'll do so for my NPI.

We'll just suggest that maybe it's got stable demand, and we will run the process to commit that.

And that's completed.

So where that now leaves us is we now have against each of our product location combination this ABC XYZ categorization.

