# Initializing Statistical Forecasting-Source Models

So taking the first two, those are the first three elements here are all going to be things which we conduct within the underlying model.

Let's just switch over to that model.

And here it is.

So this is my statistical forecasting model.

As I mentioned, the first thing, always going to be to make sure that our source models are correctly set.

So the source models I need to worry about are going to be demand analysis.

I'm going to edit that mapping.

And I will need to, find the workspace where it's coming from.

And it the demand analysis, top of the list.

Then I'll set this demand planning connection.

Let's just make sure that it's how I wanted the data management to allow this.

We should have demand planning there.

And then finally, it's going to be the connection to the data hub.

I’ll edit the mapping, and somewhere, three training exercise data hub.

Okay.

Right.

So that's my source model set.

So it's now going to be talking to the white model.

The next piece that I want to do within this model was the time settings and time ranges.

