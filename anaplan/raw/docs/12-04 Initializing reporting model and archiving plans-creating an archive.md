# Initializing reporting model and archiving plans-creating an archive

And we can now turn our attention to actually, creating an archive of the demand plan out of the demand planning model.

So to do that, the page where we will manage the demand plan archiving, sits within the demand planning application.

So I should have had that open. There it is.

And it's down here at the bottom, page 990 - Manage Archiving.

So the first thing that we want to do is to synchronize the reporting model with the demand planning model so that it's knowledge of the current period and the set of scenarios available in demand planning is correctly set.

So I will update the current period.

That changes to week 17, FY25.

So it's got the same current period as the demand planning model.

And then update the scenarios just to confirm that the list of scenarios that reporting model knows about is aligned to those within demand planning.

So now we can set about creating the archive itself.

We do that by first of all giving it a name.

So we have a, a default name, which has been, pre-populated here.

And we could give it a different name if we wanted something alternative.

We choose the, time horizon that we want to archive.

So do we want to archive, the full, the future that we have within demand planning.

Or maybe instead we just want to archive a subset of that.

So the next 52 weeks or something along those lines.

In this case, we can just leave it set to yes to archive everything.

We can then control whether the archive that we're about to take, is available in a couple of reporting subsets.

The idea here is that we may create multiple archives during a planning cycle, and only one of those is the one that we want to use when we subsequently measure forecast accuracy and other reporting requirements.

So in this case, I will leave this set to yes to include it in this waterfall and forecast accuracy subset.

The final task that before we create the archive is to select the scenarios that we want to appear within that archive.

So we may have many scenarios within demand planning.

And it might just be that only a subset of those we want to archive.

So we will choose the ones for inclusion.

In this case I'm just going to select them all.

At that point we've defined the scope of our archive.

We've given us a name so we can now create the archive.

So that is completed.

So we should now have the archive ready for use and for reporting on.

To confirm its availability, let's just check one of the reporting pages.

So into reports.

And I'm just going to look at this waterfall analysis page.

So what I can see here now is there's that snapshot week 17, FY25, version two.

And I've got some data coming through here.

So it looks to me like I have correctly archived that forecast.

Just to note, depending on the set up of your application, the columns that you see, in the table here are filtered.

And that filter may be preventing them from going beyond the current week.

The filtering is down here at the bottom.

So select here or clear it out here.

We can see that I now have restricted I can't see my data.

So the best thing to do would be to select a time period well into the future.

And then you should be able to see the data coming through.

