# Configuring and Exploring Statistical Forecasting-Overview

Exercise six to configuring and exploring the statistical forecasting parameters.

In this exercise, we will continue to progress with the statistical forecasting application by taking a look at some of the parameters which control behavior of the algorithms.

In particular, we will probably look at back-testing and how we use the back-testing methodology to select the best performing forecasting method.

We will look specifically at the concept of alpha and beta settings, because they do have an impact on things like model size and the performance of the forecasts, and will perhaps have a look or a discussion around how PlanIQ played the role within or can play a role within the stat forecast.

The output from this exercise will be, getting us into a position where we have a statistical forecast, a best fit selection, and we're ready to pass our own into demand planning.

So the task that we're going to step through, first of all, to look at these coefficients.

So the alphas and betas that some of the forecasting methods make use of.

We will look at adding and deleting those coefficients and confirming that they are available, and that they are being included in a coefficient optimization calculation.

We will then turn our attention to looking at the best fit calculation, the parameters associated with controlling that best fit selection, and ultimately how that gets used to pick and select a single forecasting method.

And then finally, we will take a look at some of the other assortment of configuration parameters within, within stat forecast.

