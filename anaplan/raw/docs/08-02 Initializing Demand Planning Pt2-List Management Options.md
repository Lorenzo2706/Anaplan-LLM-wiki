# Initializing Demand Planning Pt2-List Management Options

Taking these first two, these two list management options, what we'll do here is, first of all, look at this concept of orphans.

What we mean here or what we're talking about, are items who have lost their parent.

Throughout the application, there are a number of lists which will typically roll up into the product or customer hierarchy.

An example might be something like promotions.

Every promotion is attached to a customer and hierarchically sits as a child of the selected customer.

As we are loading and refreshing and synchronizing hierarchies within the demand planning model, if the parent is removed for whatever reason, then the underlying item will become orphaned.

It's lost its parent.

So as part of the initial project set up, we need to clean those out.

But also just in terms of the ongoing processes which are maybe automated, we will probably want to keep those, clean and continue to, clear out orphan items.

So let's revert back to the Anaplan model.

So the first couple of steps around this list item management is going to sit down at the bottom in the pages where we have some of our maintenance and administration activities taking place.

So the first one is the deletion of, orphans.

There we go.

So page 906 delete orphans where we can run the processes for that.

So if we look at this page, it's identifying, a whole range of items across various lists which are in this orphaned state.

It may be clearer actually to look at this in the underlying model.

So I'll just quickly jump to the model.

And let's take as one example the concept of promotions.

And I will just go find my promotion header list.

There it is. and open that up.

So here's an example where we have some orphans.

These were created once.

The customer that they belong to has been removed from the application.

And they’re now orphaned, along with all of these spares as well.

So that's what we're seeing within the page.

So we can run the process to clear those out.

That's done now, gone, and we should have none of those.

If we just want to go back, for instance, to the underlying model and just refresh my promotion list.

We can now see I don't have any promotions that do not belong to a customer.

In fact, I have no promotions.

So the next task now is the second list management activity of creating placeholders.

So in page nine of four create empty placeholders.

That's where I can perform this.

And again, this is across a whole number of different lists where we may want these placeholders.

So, we have building materials for new product introduction, second tier customers to set itself through type of distributor models, etc., etc., opportunities, promotions, all of these things, we want to pre-create empty placeholders for the user to then have access to.

Various parameters control how many of those we want.

So in this instance we can see, actually we want ten empty placeholders to be available against this particular list.

And five against this particular list.

We don't need to change those settings right now.

We just need to run the various processes to pre-create those.

So it will be, product related lists.

So placeholders for things which roll up into the product hierarchy.

Should be done.

And I'll scroll down further.

I'll do the same for customer related lists.

where we have lists rolling up into the customer hierarchy.

And then finally let's just do the create other lists.

Okay.

So those list titles should be created.

We've got plenty of empty placeholders for the users to then interact with as part of that planning process.

As mentioned the configuration and implementation thought process here is to first of all, make sure, we do that so that ready for when our customers first interact with the application, and then also to make sure that the associated processes are probably scheduled so that new ones are added as existing ones, get used.

