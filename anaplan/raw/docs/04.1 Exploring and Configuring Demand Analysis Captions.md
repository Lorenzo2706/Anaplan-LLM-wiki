# Exploring and Configuring Demand Analysis Captions

Since always for exploring and configuring demand analysis and completing the previous exercise, we'd initialized the demand analysis application.

We've got it set up.

We connected it to the data hub and we imported the customer hierarchy, product hierarchy, and sales order history.

In this exercise, we're going to perform some bits of configuration to then start to set up some of the business functionality to work in the way in which we want to.

So we'll explore the functionality a little bit of the application.

We'll look at some configuration parameters, not all of them.

in particular we will have a discussion around, the need or the potential for automating ABC and project analysis.

When we reached the end of this exercise, we should be in a position where we now have demand analysis, study working process validated, and the data ready to move downstream to the applications which will receive that, namely statistical forecasting and demand planning, both of which rely on demand analysis to cleanse the baseline, to be able to apply some additional manipulation and transformation to the data, and to derive some metrics and insight from that data.

So what are we going to do?

There's two main areas of activity which we will need to configure during this First of all, and we'll just use these just as two examples really of of configuring a piece of functional process.

First of all we will look at ABC exploit that analysis.

So we'll look at how we can use parameters to control that reasonably standard.

Supply chain planning concept.

We will look then at, what it means to commit the outputs from that.

So essentially to say the conclusions of the calculation.

And we’ll do that both for our product portfolio and our custom portfolio.

Once we've done that we'll then move over to seasonal profiles.

So we'll look at how we can use the data to derive seasonal indices.

And then potentially build up a library of profiles, which again is something which some of the downstream models may want to make use of.

So let's move over to Anaplan itself and back to the application.

So into the content of my demand analysis application.

We can see we've got these various functional areas.

Each lining up to the corresponding business process capability.

The first one which we want to look at is, within the section 320.

This is where I can perform product portfolio segmentation.

So that standard ABC export, that analysis.

As I step into here, so into page 320 after my starting point, we can then begin to to look at what's coming through here.

And the answer initially is not a lot.

We can see that this chart isn’t working.

So you know clearly that's something which I need to configure in order to bring this to life.

So the particular parameter which I'm interested in this instance in is this data for ABC.

This is identifying a data series, which is going to be used as the basis for assessing the relative importance of each of my products.

And in this case I want to set that parameter to being selected.

History (U).

(U) meaning in this instance volumes, units.

So I'll just set that and I'll refresh the chart.

And now you can see my parameter chart is calculated through.

I can see that orange juice smooth 12 count 625 mil bottles is my largest selling product by volume.

As I scroll down to the bottom here.

We can see from this couple of pieces of analysis that, and then using that Pareto assessment to identify my ABC, my really important most of the important, not so important products, but also separately based upon this variability parameter, I'm scoring the products in terms of their demand volatility X being stable through to the Z being, really quite unpredictable and volatile demand.

In many places within the applications where we see parameters like these.

They are often also housed within the global configuration area.

So page 900 would be the place where I can go to see all of the parameters that were available for me when it comes to setting up my demand analysis application.

And here we can see there's the one that we've just been changing on the individual page.

But what do I want to do as part of my configuration task in this instance?

So having allowed the calculation to achieve this ABC XYZ scoring for me I'm actually going to go and step to another page.

Page 326 where I have the opportunity to save that.

Save it. Meaning?

Okay, I've got a dynamically calculated answer, but I now need to capture that they recorded piece of product master data.

So that's the calculation.

I could manually override it if I wanted to.

And this is the sort of saved version which will potentially be used downstream.

How do I do that?

Well, in this instance it's via a process.

So data process is going to need to be run.

So I'll kick that off.

And then I will just move through to completion.

Okay.

We can now see they are saved.

I'm going to do the same thing to the XYZ scoring of these products.

So add another page Edit & Commit XYZ I could select it exactly the same idea.

What are we currently calculating?

Do I want to override it.

And then what's the saved value to use downstream.

I need to refresh that saved value by running a process.

So why is this important for the initial implementation and configuration?

I guess two reasons.

Firstly, it's important to check is working.

And as part of playing and presenting back to your customer, it's very likely that you need to make sure the saved values are stored.

They do get used downstream.

It's an all important part of the project, though, also to have a discussion and to make a decision around this activity for saving the data.

this can be automated.

So not uncommon to use Cloud works with this to, to run these processes on a nightly basis, to refresh and to restore the current values.

But actually in many instances that might not be an appropriate thing to do.

And that's a discussion that business process discussion with needs to be had.

When might not be good?

It's because in particular, the ABCXYZ scores will get used within inventory planning as the basis for inventory policy settings.

So the policy which we set for an AX could be different to a BX or the other settlements.

So often a deliberate review and business process governance for when those get updated might be a wise thing to do, so that not every night we necessarily refreshing them and potentially changing our target inventory levels.

Just to finish off this piece of the ABCXYZ analysis I'm just going to dig down and do the same thing for the customer portfolio story.

So into page 340 I'm going to check that my parameter is set how I want it to be.

That's the wrong page.

So it's page 300.

Okay.

So I'm using my selected history units.

So my historic volumes is how I'm scoring my customers.

But create a chart is working so I can see Tesco is by far my biggest customer.

Now I'm just going to go to the page where I can run the same process to commit that ABC.

Conclusion.

Right.

And that's finished.

So just reverting back to the the PowerPoint.

Let's check where we are at that. Okay.

So we've done this first block of all configuration tasks.

So we have our A, B, C and z calculated the parameter controlling that.

And then we have run some processes manually to save those.

And we talked about whether it might be wise or not to automate that process on an ongoing basis.

