# Initializing reporting model and archiving plans-auto archiving

So that's brought us through to the tasks that we need to conduct directly within the application.

Just to close off this exercise, let's just step over.

We just created an archive there by manually setting its scope and then running the process.

There are also options for automating that activity.

So if your customer if part of the implementation, they want to see the archive created on some sort of scheduled basis, maybe once a week at the weekend or something like that, then auto archiving is possible.

To achieve that., if we just go back to the page where we ran the archiving and scroll to the bottom, this is where we can populate the details that define the scope of what would be included in one of those scheduled archives.

And then there is a process which could be automated through cloud workflow or workflow.

Whether you go down the route of automating this or leaving it as a manual is purely down to the preference of your customer.

In some instances, it may be appropriate to have it as a manual activity so that the archive is taken once the consensus planning meeting has completed.

And therefore it becomes a very conscious step with the administrator.

In other cases, that periodic scheduled approach may be appropriate.

That brings us to the end of this exercise.

If we just step through, we have initialized the reporting model.

So this additional model where we hold the archives, and we have created an archive and we've explored or discussed the automation of that archive it.

