# Initializing Statistical Forecasting-Overview

Exercise five.

Initializing the statistical forecasting application.

In this exercise, we're going to start to set up the statistical forecasting model and associated application.

That will involve connecting it to the source models where it needs to receive data structures from, namely the data hub and the demand analysis model bringing over those data sets and associated hierarchies.

And then we'll do one particular task around some structural configuration associated with some good level planning.

And we'll explain why we do that in a moment.

The outcome of this exercise should be that we get to a position where the statistical forecasting model application is beginning to produce a forecast, and we're ready to be able to exploit that forecast in some of the downstream activities.

So what's this going to look like?

So first of all, very similar to actually when we set up demand analysis.

You know, we'll first all need to set our source models to be the correct target.

We will review our time settings and time ranges to get those to be aligned in the way in which we want them to be.

We will bring over the hierarchies from the data hub, data from the data hub.

And then we should be in a position where we can sense check, that a forecast is being produced.

Once we've got that forecast, we will then look a little bit around at this structural configuration activity associated with, aggregate level planning and associated disaggregation.

