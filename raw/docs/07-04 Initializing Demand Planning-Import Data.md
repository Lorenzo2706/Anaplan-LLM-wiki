# Initializing Demand Planning-Import Data

Great.

So with the hierarchies in, we can now move on to bringing data over from the various models.

Just before we run the processes themselves, let's just touch on the scope of the main data sets which come into demand planning from other places.

From the data hub, demand planning will show us the demand history.

It will use the data hub to tell it what the current period is.

And it will also include the open orders as well., if we have open orders in our sales history.

There are various other data sets such as a budget.

If we wanted to use that for comparison purposes, a marketing plan, if we wanted to source some data for a top down plan and some contracted customer demand and constrained supply for particular areas of demand planning, functionality,.

From demand analysis, we will bring over the corrected and chained history.

So Data Hub gives us the pure, original history of demand analysis.

We get the output from that history collection and training processes.

We will bring over the seasonal profiles.

So we created some of those within demand analysis.

We may then want to use them with things like new product introduction.

And we will bring over other metrics associated with our products and customers, such as the ABC, XYZ scoring.

That product classification and other bits and pieces on statistical forecasting.

Clearly we will bring over the stat forecast itself, but we will also include other, information to help us describe or context of information related to that stat forecast.

So how that forecast scored when we measured its forecast accuracy on the back test, what period of history we used within stat forecast and things such as what the best fit selected method itself was.

So they are the data sets.

Now let's go back to Anaplan and run those into demand planning.

So staying within my data hub application various data sets, we can see are managed down here at the bottom.

So I'm going to first of all run this update all demand transactions.

And that's going to bring the data into demand planning from the data hub.

So that should be demand planning now filled with data from data hub and the couple of pages where I will deal with the imports from the other models.

So next on page 236DP, this is where I'm going to pull into demand planning the various pieces of data to get some demand analysis.

And we can run the process there.

Okay. That looks good.

And then finally we will bring the statistical forecast and associated metrics in by running the process on page 238 and running the process update system forecasting.

So that should be all of the data now within demand planning that it needs to source from various other models.

The final little task within this exercise would just be to take a look at one page to confirm that we do indeed have that data.

So within the demand planning application, I'm going to take a look at a page which is at the heart of demand planning.

And that is page 350 demand planning.

Okay.

So looking at this, I can see my hierarchies already confirmed; they are working.

I just need corrected history.

So that will come across from the demand analysis model.

And I can see a, shown up here is a baseline plan.

That baseline plan a parameter it is probably telling me it comes from the system forecast.

So that means the connection with the stat forecast model is also working.

So that completes this exercise.

We have the model up and running.

We have the hierarchies in, we have the data in, and we're now ready to start demand planning.

