# Overlaying Events onto the Demand Baseline-Caniballization

So that's got us through to the point where we have, modeled the product that we're launching.

So we've set it to be planned for based on NPI, and we've imported associated details.

The last element which we just want to complete is now the cannibalization modeling.

So we will first of all create cannibalization placeholders.

And then we will input some assumptions against those in order to model out the impact on, cannibalized products, which we'll then see in our main demand end page.

So for the creation of the placeholders, this is another example of where we have those empty list items ready to be used up to plan against.

We can see that’s from there.

Now, so we're just going to need to run the placeholder creation because, you know, having only just created the launch product, the new dummy product, we need to create some of its cannibalization children.

So we will run this.

So that should be completed.

So now if we just followed through the process, back to page 222.

That's the page where we capture all of our NPI assumptions.

Scrolling down further here, we’ve got NPI product selected, we can now mark another product in the portfolio as one that will be cannibalized.

So let's, take this first one with Bits here.

Select the product and then both by clicking on that entry there will sink the table below.

we should now see the cannibalization relationship.

We can enter the associated, pieces of information for planning that.

So now we can begin to define how the cannibalization is going to operate.

So we select the cannibalization strategy.

So this is just two different ways of articulating the impact of cannibalization.

Expressing it in terms of how much of the new volumes are going to come up for that existing product, or how much of the existing product volumes are going to be impacted.

So we'll select NPI volumes and say that 25% of everything that the new product achieves.

It's going to be to the detriment of this existing product.

So if we now move back over to the main demand planning page, 350, then we should see that net impact coming through.

So I think it was Orange with bits, that we selected.

So we can now see coming up with that particular product.

It's the impact of that cannibalization that we just saw.

And equally we would be able to see it down within the main table as well.

So that’s the main element of this particular, exercise.

So as we walk through this, we've created a temporary product in the data hub.

We have then input and modeled out it's, new product volume.

So the demand that the new product will see.

We've modeled out the cannibalization on existing products.

The last point of discussion is just want to highlight, the scenario in which further down the line, that new product is then assigned that code within the ERP.

So some thought needs to be made during the project and during the configuration as to, the business process for which that temporary product will inherit the code of the new product.

If we just allow that new product to flow into the application, then essentially we will have the temporary product and the new item as per the ERP, as separate items.

And we would have the original NPI plan in that temporary product.

And in reality, we probably want to see it against official new product.

So there are some steps within the data hub that allow us to assign that official ERP code to our temporary product, and then replace the temporary code that we had in the situation.

