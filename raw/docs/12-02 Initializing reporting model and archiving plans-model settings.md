# Initializing reporting model and archiving plans-model settings

So as we step over to Anaplan, the first place we are going to go to is the reporting model.

This is the first time that we have opened up this particular model.

The first step within that reporting model, as with any model, when we first initialize it, is to correctly set up the source model connections so that it is connected to the corresponding set of models within the architecture.

So I'll do my data hub.

Demand analysis model.

Demand planning.

And just to be sure, we'll also complete the two connections that we see here for the supply planning model.

Having completed that, the next task is to define the time settings.

So normal time setting definitions.

We’ll remind ourselves that we wanted the current year to be FY25, with three years history, and one year future.

So that's what we'll set up, FY25, three years history, one year future.

Then we'll do the time range.

So we have one time change here titled future.

We want that to be FY25 with two years.

So there's the time range.

Let's edit that.

Beginning FY25, and to be two periods long.

Just make sure that you don't make the same mistake that I made just there.

Just remember that we need to retain the total of all periods.

Boolean tick that we see here.

Then we should be able to successfully save the time range.

Okay, so that should complete the bits that we need to do in the model itself.

