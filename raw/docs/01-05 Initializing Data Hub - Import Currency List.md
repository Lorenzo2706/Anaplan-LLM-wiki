# Initializing Data Hub - Import Currency List

So we cleared out the data hub.

We've updated our time settings.

We've been imported for that, customer hierarchies.

We've looked at those hierarchies to confirm that they appear as we would expect them to.

So the next step, which I want to do is just to refresh a list of currencies.

There's something which got deleted when I ran that mass deletion process.

And we will need that later on.

So I think we will just do that now.

Just while where in the steps of interacting with the data.

So back to my UX application.

Now that list of currency imports sits on another page.

It's within the same section for the very first data imports.

Category.

We've just been on this page 106 hierarchies.

There's page 102, which allows me to deal with a number of the other flat lists that we import into the application.

here's the currency one.

Unlike the hierarchy with these, because we're just importing flat lists.

We don't have that two step process where we bring in to a staging area and then build the hierarchy.

We just do one single action to import the list.

So if I select my currency process, I can find the file in the same place so that my currency file.

Let's open that up.

Run the process.

And we’re done.

Again of course, if I want to look at that, to confirm that it's being built.

There's a page.

It's this page 400 and that’s hopefully where I can see indeed, I have a short list of currencies within my application that.

