# Initializing reporting model and archiving plans-hierarchies and data

If we just revert back to our set of steps, we have set up the source model.

And we have correctly set up our time settings and time ranges.

So now we will save the model with hierarchies and data.

And we will do that through the data hub application.

So we'll just go and open up our data hub application.

As with all other models, the pages for importing into those models sits within the data hub application.

And down here within this 216 section, we see the export two reporting.

Opening up the page.

This allows us to update all of the hierarchies that we see here.

And also some of the transactional data.

So I'll just go ahead and run the update all hierarchies process which will bring in the product and the customer hierarchy that I want.

That’s completed Just one error here.

Let's just confirm, that's nothing to worry about.

No, I don't think it is.

It's related to locations, the location hierarchy, and we're not worried about that right now.

So, having updated the, hierarchies within the reporting model, we can now go and, also update the data.

So this is now to pull through the sales order history.

So the demand history straight from the data hub into the reporting model.

And we'll run this update selected history process.

It's on the same page we were looking at.

So it's this process that we see here on the top right.

So we'll just run that.

Right. So that's completed.

So that now leaves us in the position where we have set up and seeded the reporting model with everything which it needs,

